class Planet:
    pass


earth = Planet()
mars = Planet()

earth.name = "Earth"
print(earth.name)

# In python attributes to the object can be added dynamically.
# Same is not possible in Java.
# However, added dynamic attributes to the object are only specific to that instance
